<%- include("head") %>
<body>    
    <section id="sidebar">
        <% if (role === 2) { %>
            <%- include("sidebarmember") %>
        <% } else if (role === 1) { %>
            <%- include("sidebaradmin") %>
        <% } %>
	</section>

    <section id="content">
        <nav>
            <%- include("navbar") %>
        </nav>

        <main>
            <%- body %>
        </main>
    </section>
    <script src="script.js"></script>
</body>
</html>